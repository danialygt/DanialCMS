@model RenameFileViewModel
@{
    ViewData["Title"] = "ویرایش فایل";
}




@if (Model != null)
{

    <div class="row d-flex justify-content-center ">
        <div class="col-lg-6">
            <div class="card">
                <form method="post" asp-controller="FileManager" asp-action="Update" asp-route-Id="@Model.Id">

                    <div class="card-header">
                        <i class="fas fa-file mx-1"></i>
                        <small>ویرایش</small>
                        <strong class="card-title pl-2">فایل</strong>
                    </div>
                    <div class="card-body">
                        <div class="mx-auto d-block">

                            <h5 class="text-sm-center mt-2 mb-1">@Model.Name</h5>
                            <div class="form-group">
                                <label asp-for="@Model.Name" class="form-control-label"></label>
                                <input type="text" class="form-control" asp-for="@Model.Name" value="@Model.Name" autofocus required maxlength="30" >
                            </div>
                            <div class="form-group">
                                <div asp-validation-summary="All" class="text-danger mr-3"></div>
                            </div>
                        </div>
                    </div>
                    <div class="card-footer card-footer__button">
                        <input type="submit" class="btn btn-warning btn-sm px-3" value="ویرایش" />
                    </div>
                </form>
            </div>
        </div>
    </div>



}
else
{
    <div class="row d-flex justify-content-center">
        <div class="alert alert-danger" role="alert">
            فایل یافت نشد!
        </div>
    </div>
}